# Tools

Reusable scripts and command-line utilities for the macos-scripts project.

## Purpose

Keep useful building blocks in one place.

## Contents

- `scripts/` — standalone helper scripts
- `cli/` — command-line tools and wrappers
- `mac-terminal-guide/` — source folder for terminal guide content
- `docs/mac-terminal-guide.html` — generated local HTML guide

## How to run

### CLI tools

Run from the project root:

```bash
bash tools/cli/ai-mode.sh





