# v2.0.0 Sequencing — Toward a Governed mqlaunch Runtime

**Status:** Planned
**Target release:** `v2.0.0`
**Theme:** Stop shipping three overlapping runtimes; ship one governed runtime
with an explicit migration boundary.
**Owner:** `mqlaunch`

Linked from [ROADMAP.md](../../ROADMAP.md). This doc sequences the architecture
remediation workstreams (owners, dependencies, gating, risk) that together gate
the `v2.0.0` cut. The per-initiative detail lives in its own plan; this is the
ordering layer above them.

Grounded in the codebase audit (2026-07-05) and verified runtime facts in
[P1-runtime-authority.md](P1-runtime-authority.md).

> **Naming:** the ROADMAP initiative titled *Phase 12 / v2.0.0* is the release
> milestone. It is delivered by **Steps 7–12** below — the roadmap's "12" is the
> milestone label, not a step number. To avoid a clash, this doc numbers its own
> workstreams as `Step N`, and reserves `P1 Phase N` for the phases defined in
> [P1-runtime-authority.md](P1-runtime-authority.md).

---

## Workstreams

Each workstream is a numbered step toward v2.0.0. Steps are ordered by
dependency, not by wish. `Not before` names the earliest gate a step may start;
`Depends on` names the hard prerequisite.

| Step | Workstream | Owner | Depends on | Not before | Risk | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 7 | Dead-code + install hardening (audit P2/P7 quick wins) | mqlaunch | — | — | Low | Done (PR #32) |
| 8 | Git-status / UI dedup to one source of truth (audit P3) | mqlaunch | — | — | Low | Done (PR #32) |
| 9 | Runtime authority declaration — `docs/AUTHORITY_MAP.md` (P1 Phase 1) | mqlaunch | Step 8 | — | Low | In progress |
| 10 | Architecture freeze — no new v1 / bridge / dup-UI debt (P1 Phase 2) | mqlaunch | Step 9 | Step 9 | Low | Planned |
| 11a | Monolith de-layering — pull concerns out of `mqlaunch.sh` (audit P4) | mqlaunch | Step 9 | Step 10 | Medium | Planned |
| 11b | `eval "$args"` → array passing in command surface (audit P5) | mqlaunch | — | — | Low | Planned |
| 11c | Observability — replace silent `2>/dev/null` swallowing with a debug-gated log helper (audit P6) | mqlaunch | — | — | Low-Med | Planned |
| 12 | Compat removal — migrate performance off v1, shrink bridges, delete v1 (P1 Phase 4) | mqlaunch | Step 11a | Step 10 + Step 11a | **High** | Planned |
| — | **v2.0.0 cut** | mqlaunch | Step 12 | Step 12 green | — | Gated |

`11a/11b/11c` share a milestone but are independent of each other: `11b` and
`11c` can land any time after Step 10's freeze rules exist; `11a` is the one that
unblocks Step 12.

---

## Dependency graph

```text
Step 7 (quick wins) ── done
Step 8 (P3 dedup)   ── done
        │
        ▼
Step 9  Authority map  ──────────────┐
        │                            │
        ▼                            ▼
Step 10 Freeze drift           Step 11b eval→"$@"    (independent)
        │                      Step 11c observability (independent)
        ▼
Step 11a Monolith de-layering
        │
        ▼
Step 12 Compat removal (v1 deleted)
        │
        ▼
   v2.0.0 cut
```

The critical path is `9 → 10 → 11a → 12 → v2.0.0`. Everything else is
parallelizable and must not block the cut.

---

## Gating rules (Not before)

* **Step 12 must not start before Step 11a.** Deleting v1 while the monolith
  still owns performance/git/network logic risks migrating behavior into the
  wrong place. First give each concern a live home, then cut v1.
* **No step past 10 may reintroduce a v1 dependency.** Once the freeze
  (Step 10) is in effect, any new live→v1 edge is a regression, not debt.
* **v2.0.0 must not cut before Step 12 is green** — i.e. no live path reaches
  `terminal/mqlaunch-v1/`, verified by a lint/test that fails on the forbidden
  dependency direction.

---

## Risk register

| ID | Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- | --- |
| R1 | Migrating performance off v1 changes observed metrics/output | Medium | High | Snapshot current `mq-performance-menu` output as a golden test before Step 12; diff after |
| R2 | Monolith de-layering (11a) silently changes launcher behavior | Medium | High | Move one concern per PR; keep `headless-smoke` + selftest green each step; no batching |
| R3 | Hidden fallback path reaches v1 after the freeze | Medium | Medium | Add a CI check that greps live menus/launchers for `mqlaunch-v1` and fails (Step 10 deliverable) |
| R4 | `eval → "$@"` (11b) alters argument parsing for existing users | Low | Medium | Treat as a flagged behavior fix, not a refactor; document in changelog |
| R5 | Scope creep turns v2.0.0 into a rewrite | Medium | High | Out-of-scope list in [P1-runtime-authority.md](P1-runtime-authority.md) is binding; no UI redesign, no new features |
| R6 | Bridges deleted while still carrying live traffic | Low | High | Remove a bridge only after grep proves zero live callers; reclassify before delete |

---

## Definition of done (v2.0.0)

`v2.0.0` may be cut only when all of the following hold:

* [ ] Steps 9–12 complete and their exit criteria met
* [ ] `docs/AUTHORITY_MAP.md` is current and classifies every runtime path
* [ ] No live menu or launcher sources `terminal/mqlaunch-v1/*` (CI-enforced)
* [ ] `terminal/mqlaunch-v1/` is either deleted or classified `DEPRECATED` with no live reachability
* [ ] `mqlaunch.sh` is orchestration-first; rendering, git, network, obsidian, and recommendations concerns have named owners outside the monolith
* [ ] One dashboard/UI authority; no duplicate rendering logic
* [ ] Command-surface `eval` on user input replaced with array passing
* [ ] Silent error swallowing on live paths replaced with debug-gated logging
* [ ] `scripts/install-smoke.sh` and all smoke tests green

---

## Expected outcome

At `v2.0.0`, mqlaunch is one governed runtime: a thin launcher that orchestrates
a single live menu/UI/library layer, with no hidden legacy runtime and no
guesswork about where a change belongs.
